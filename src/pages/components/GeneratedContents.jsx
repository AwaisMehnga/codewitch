export default function GeneratedContents(props) {
  return (
    <textarea
      className={props.className}
      name={props.id}
      id={props.id}
      value={props.dataRes}
    ></textarea>
  );
}
